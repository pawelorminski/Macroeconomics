# Skopiuj do .env.local. UWAGA: to jest klucz publiczny anon (chroniony przez
# Row Level Security w Postgresie), NIE klucz service role z etl/.env.example
# — service role nigdy nie powinien trafić do kodu klienckiego.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
